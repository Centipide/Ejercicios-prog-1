#include<stdio.h>
#include<math.h>

int main()
{
    float ev1, ev2, promedio;
    printf("Ingresar la nota de la primera evaluacion del alumno (mayor a cero y menor a 10)");
    scanf("%f",&ev1);
    printf("Ingresar la nota de la segunda evaluacion del alumno (mayor a cero y menor a 10)");
    scanf ("%f",&ev2);
    promedio=(ev1+ev2)/2.00;
    printf("La nota promedio del alumno es %.2f\n",promedio);
    
    system("pause");
    return 0;
}
